<% cache([question.updated_at, question.answers_count]) do%>
<div class = "light-top-border">
	<div>
		<h3><b><%=link_to question.title, question_path(question),class: "question-heading"%></b></h3>
	</div>
	<div class="question-content">
		<p> <small><%= question.content %></small> </p>
	</div>
	<div>
		<p class="text-left light-text"> <%= question.answers_count%> answers &nbsp; 0 comments	</strong>
		</p>
		<p class="text-right light-text"> <small>Asked on: <strong><%= question.created_at.to_formatted_s(:long_ordinal) %></strong></small> by <strong><%= question.user.first_name%>	</strong>
		</p>
	</div>
</div>
<%end%>

